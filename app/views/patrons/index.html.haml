%h1 Listing patrons

%table
  %tr
    %th First name
    %th Last name
    %th
    %th
    %th

  - @patrons.each do |patron|
    %tr
      %td= patron.first_name
      %td= patron.last_name
      %td= link_to 'Show', patron
      %td= link_to 'Edit', edit_patron_path(patron)
      %td= link_to 'Destroy', patron, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Patron', new_patron_path
